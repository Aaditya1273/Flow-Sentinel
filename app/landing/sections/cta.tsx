'use client'

import { motion } from 'framer-motion'
import { GlowButton } from '../hero'

export function CtaSection() {
  return null // CTA is handled inline in the footer section for walrus-exact layout
}
